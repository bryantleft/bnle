{
	"name": "bruhs",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"dev:portfolio": "turbo run dev --filter=portfolio",
		"dev:snip": "turbo run dev --filter=snip",
		"check:portfolio": "turbo run check --filter=portfolio",
		"check:snip": "turbo run check --filter=snip"
	},
	"devDependencies": {
		"turbo": "2.5.3"
	},
	"packageManager": "pnpm@10.11.0",
	"engines": {
		"node": ">=20"
	}
}
